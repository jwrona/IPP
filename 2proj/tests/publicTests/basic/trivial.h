// Pár triviálních příkladů, včetně komentáře, který obsahuje text s diakritikou
// (soubor je v kódování utf-8, ěščřžýáíéóů).
bool isBraceFormat();
unsigned int getDirectiveNestLevel(void);
void cppInit(const boolean * state, char hasAtLiteralStrings);
void cppUngetc(char *a, ...);
